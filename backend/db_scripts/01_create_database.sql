-- This script creates the main database for the library system.
-- It should be run by a PostgreSQL superuser if the database does not already exist.
-- Example: psql -U postgres -c "CREATE DATABASE library_system;"

CREATE DATABASE library_system;
